name: Deploy to OVHcloud

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Build and Deploy to OVHcloud
    runs-on: ubuntu-latest

    steps:
      - name: Deploy with Docker
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSHKEY }}
          script: |
            cd /home/ubuntu/projects/cristian-sierra-ngx

            # Pull latest code from repository
            git pull origin main

            # Create .env file from GitHub secrets
            cat > .env << EOF
            NODE_ENV=production
            PORT=3000
            API_TOKEN=${{ secrets.API_TOKEN }}
            API_URL=${{ vars.API_URL }}
            PUBLIC_TURNSTILE_SITE_KEY=${{ vars.PUBLIC_TURNSTILE_SITE_KEY }}
            TURNSTILE_SECRET_KEY=${{ secrets.TURNSTILE_SECRET_KEY }}
            N8N_API_URL=${{ vars.N8N_API_URL }}
            N8N_API_USER=${{ secrets.N8N_API_USER }}
            N8N_API_PASSWORD=${{ secrets.N8N_API_PASSWORD }}
            EOF

            # Deploy with docker-compose
            docker compose up -d --build

            echo "Deployment complete!"

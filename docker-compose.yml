services:
  app:
    build:
      context: .
      args:
        - PUBLIC_TURNSTILE_SITE_KEY
        - N8N_API_URL
        - N8N_API_USER
        - N8N_API_PASSWORD
        - API_URL
        - API_TOKEN
        - TURNSTILE_SECRET_KEY
      dockerfile: Dockerfile
    container_name: cristian-sierra-app
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=${PORT:-3300}
    ports:
      - '${PORT:-3300}:${PORT:-3300}'
    networks:
      - nginxproxymanager-network
    restart: unless-stopped
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "require('http').get('http://localhost:3300', (r) => {process.exit(r.statusCode === 200 ? 0 : 1)})"
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  nginxproxymanager-network:
    external: true
